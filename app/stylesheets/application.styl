@import 'nib'
@require '_colors'
@require '_mixins'
@require '_animations'

@require '_cells'
@require '_containers'
@require '_zones'

html, body, #root, .xray
  min-height: 100%
  @extend $full-height

body
  background-color: $black

.receptor-url-modal
  input[type="text"]
    @extend $full-width


