@import 'nib'
@require '_colors'
@require '_mixins'

.xray
  @extend $zero
  background-color: $black

.receptor-url-modal
  input[type="text"]
    @extend $full-width

.cells
  color: $white
  .cell
    background-color: $dark-gray
    margin: 5px
    padding: 5px
  ul
    margin: 0
